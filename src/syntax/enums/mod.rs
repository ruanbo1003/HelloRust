#![allow(dead_code)]

mod enum_basis;
mod enum_option;
mod enum_match;
mod if_let;

pub fn enum_tests() {
    // enum_basis::tests();

    // enum_option::tests();

    // enum_match::tests();

    if_let::tests();
}
