#![allow(dead_code)]

mod enum_basis;
mod enum_option;
mod enum_match;
mod if_let;
mod alias;
mod list;

pub fn tests() {
    // enum_basis::tests();

    // enum_option::tests();

    // enum_match::tests();

    // if_let::tests();

    // alias::tests();

    list::tests();
}
